#include <iostream>
//incomplete
using namespace std;

int main(){

    // freopen("photo.in", "r", stdin);
    // freopen("photo.out", "w", stdout);

    int n;
    cin>>n;

    vector<int> b(n);

    for(int i=0;i<n;i++){
        cin>>b[i];
    }

    for(int a1=1;a1<n;a1++){
        vector<int> a(n);
        vector<bool>seen(n+1,false);
        bool ok=true;
    }

}